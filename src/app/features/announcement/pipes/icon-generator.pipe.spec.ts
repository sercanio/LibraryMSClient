import { IconGeneratorPipe } from './icon-generator.pipe';

describe('IconGeneratorPipe', () => {
  it('create an instance', () => {
    const pipe = new IconGeneratorPipe();
    expect(pipe).toBeTruthy();
  });
});
